import React from 'react'

export default function About() {
    return (
        <React.Fragment>
            {/* can use Fragments when you want to group the elements of a component without using a div. */}
            <h1>About</h1>
            <p>This is the TodoList app v1.0.0 which I am building as part of a React Crash course</p>

        </React.Fragment>
    )
}
